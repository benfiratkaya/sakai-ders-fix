import {Container} from "react-bootstrap";

const Footer = () => {
  return (
    <div className="text-dark bg-light py-3 mt-5 text-center">
      <Container>
        Sakai'de bulunan derslerin, ses sorununu çözmek için yapılmıştır.
      </Container>
    </div>
  );
};

export default Footer;
